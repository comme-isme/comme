//import 할 거 하기

Public class MaintActivity extends AppCompatActivity{

	LinearLayout layout;

	protected void onCreate(Bundle savedInstanceState){
		super.onCreate(savedInstanceState);
		setContentView(R.layout.activity_main);


		layout=(LinearLayout)findViewById(R.id.mainlayout);
	}
	
	public void PaintColorChange(View v){
		MyDraw.PaintColor=Color.rgb(random.nextInt(255),random.nextInt(255),random.nextInt(255));
	}
	
	public void BackgroundColorChange(View v){
		MyDraw.BackgroundColor=Color.rgb(random.nextInt(255),random.nextInt(255),random.nextInt(255));
		layout.setBackgroundResource(MyDraw.BackgroundColor);
	}
	
	public void Clear(View v){
		MyDraw.BackgroundColor.rgb(255,255,255);
		layout.setBackgroundResource(MyDraw.BackgroundColor);
	}
	
}



Public class MyDraw extends view{
	int sx,sy,ex,ey;
	int cx,xy;
	Random rnd=new Random();
	int CircleSize;
	Paint Paint=new Paint();
	Paint BackgroundColor=new Paint();
	
	PaintColor.setColor(Color.black);
	BackgroundColor.setColor(Color.white);
	
	Public boolean onTouchEvent(MotionEvent event){
		cx=(int)event.getX();
		cy=(int)event.getY();
		If(rnd.nextInt(10)>5){
			CircleSize=rnd.nextInt(145)+5
			Paint.setStrokeWidth(1f);//여기가 두께
			Paint.setStyle(Paint.Style.Stroke);
			canvas.drawCircle(cx,cy,CircleSize,Paint);
			
		} else{
			CircleSize=rnd.nextInt(145)+5
			Paint.setStyle(Paint.Style.Fill);
			canbas.drawCircle(cx,cy,CircleSize,Paint);
			
		}
	
	}
	
	Public boolean onDrag(View v, DragEvent event){
		switch(event.getAction()){
			
			case DragEvent.ACTION_DRAG_STARTED:
				sx=(int)event.getX();
				sy=(int)event.getY();
				return true;

			case DragEvent.ACTION_DRAG_ENTERED:
				case MotionEvent.ACTION_MOVE;
				ex=(int)event.getX();
				ey=(int)event.getY();
				Canvas.drawLine(sx,sy,ex,sy,BackgroundColor);
				return true;
			case DragEvent.ACTION_DRAG_EXITED:
				return false;
		}
	}
